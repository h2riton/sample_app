<%= form_for @tournament do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    Titre du tournoi
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit "Valider" %>
  </div>
<% end %>